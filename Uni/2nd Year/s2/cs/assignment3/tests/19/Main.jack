class Main
{
    function void main()
    {
        var int a,b,c,d,e ;

        let a = 0 * ( 45 / 0 ) | -1 ;
        let b = d > d ;
        let c = (b * X.x() / 0 ) & 0 ;
        if ( ~(~(d = d)) )
        {
            let d = Other.called() * 0 + 4 - (-1 * -45) ;
            let e = b & b ;
        }
        return ;
    }
}
